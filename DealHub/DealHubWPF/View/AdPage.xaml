<UserControl x:Class="DealHubWPF.View.AdPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DealHubWPF.View"
             mc:Ignorable="d" d:DesignWidth="800" Height="532">

    <Grid>
        <!-- Основний вміст -->
        <Grid Background="White" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Верхня панель -->
            <Border Grid.Row="0" CornerRadius="15" BorderThickness="1" Background="#EAC4C4" Height="47" Margin="10,20,10,10">
                <DockPanel Background="Transparent">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Command="{Binding RegUserCommand}" Style="{StaticResource RegButtons}" Background="Transparent" BorderThickness="0" Cursor="Hand">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Image Source="/Images/updatelogo.png" Width="69" Height="53" Margin="0,0,5,0" />
                                <TextBlock Text="DealHub" FontSize="18" FontWeight="Bold" Margin="10,10,0,0" Width="103" Foreground="Black"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Command="{Binding ProfileCommand}" Style="{StaticResource LogButtons}" BorderThickness="0" Background="Transparent">
                            <TextBlock Foreground="#65558F" FontFamily="Segoe MDL2 Assets" Text="&#xE77B;" FontSize="24" Margin="10"/>
                        </Button>
                        <Button Command="{Binding ChatsCommand}" Style="{StaticResource LogButtons}" BorderThickness="0" Background="Transparent">
                            <TextBlock Foreground="#65558F" FontFamily="Segoe MDL2 Assets" Text="&#xE8BD;" FontSize="24" Margin="10"/>
                        </Button>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- Основний контент -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="400"/>
                </Grid.ColumnDefinitions>

                <!-- Зображення та опис -->
                <StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,20,0">
                    <Image Source="{Binding Image}" Width="300" Height="300" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" FontSize="16" FontWeight="Normal" HorizontalAlignment="Center" Height="73" Width="294"/>
                </StackPanel>

                <!-- Інформація про товар -->
                <StackPanel Grid.Column="1" VerticalAlignment="Top" Margin="0,20,10,0">
                    <Border Background="#F5C8D0" CornerRadius="15" Padding="20" Height="190">
                        <StackPanel>
                            <DockPanel>
                                <TextBlock TextWrapping="Wrap" Text="{Binding Title}" FontSize="18" FontWeight="Bold" Height="53" Width="241" />
                                <TextBlock TextWrapping="Wrap" Text="{Binding Price, StringFormat={}{0} грн}" FontSize="18" FontWeight="Bold" Width="106" />
                            </DockPanel>
                            <Button Style="{StaticResource CategoryButtons}" Content="Повідомлення" Background="#6C3483" Foreground="White" Height="32" Margin="0,15,0,5" BorderThickness="0" Click="Button_Click"/>
                            <Button  Style="{StaticResource CategoryButtons}" Content="Оформити доставку" Background="#6C3483" Foreground="White" Height="32" BorderThickness="0" Click="Button_Click_3"/>
                        </StackPanel>
                    </Border>

                    <Border BorderThickness="1" BorderBrush="LightGray" Margin="0,60,0,0" CornerRadius="15" Height="85" Width="384">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,-1">
                            <Button Command="{Binding AnotherUserCommand}" Background="Transparent" Style="{StaticResource RegButtons}">
                                <Image Source="/Images/profileico.png" Width="94" Height="77" Margin="0,0,10,0"/>
                            </Button>
                            <StackPanel>
                                <TextBlock Text="{Binding OwnerNickname}" FontWeight="Bold" Height="23"/>
                                <TextBlock Text="{Binding ReviewsCount, StringFormat='Відгуків: {0}',  UpdateSourceTrigger=PropertyChanged}" FontSize="12"/>
                                <Button Style="{StaticResource CategoryButtons}" Content="Залишити відгук" FontSize="12" Padding="2,0" Margin="0,5,0,0" Width="120" Background="#C39BD3" Foreground="White" BorderThickness="0" Height="17" Click="Button_Click_2"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
                <Button x:Name="UserButton1"
            Style="{StaticResource RegButtons}"
            BorderThickness="0"
            Background="Transparent"
            Width="42"
            Height="42"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Click="UserButton1_Click"
            Margin="0,20,359,0" Grid.ColumnSpan="2">
                    <Image Source="/Images/flag.png" Width="27" Height="27" />
                </Button>
            </Grid>
        </Grid>

        <!-- ChatBox поверх усього -->
        <Border x:Name="ChatBox" Background="#FFF8F8F8" BorderBrush="Gray" BorderThickness="1"
                HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,20,20"
                Width="400" Height="246" Panel.ZIndex="10" Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Background="#F5C8D0" CornerRadius="15" Height="36" Margin="10" Grid.Row="0">
                    <StackPanel Orientation="Horizontal">
                        <Button Command="{Binding AnotherUserCommand}" Background="Transparent" Style="{StaticResource RegButtons}">
                            <Image Source="/Images/profileico.png" Width="45" Height="32" Margin="0,0,10,0"/>
                        </Button>
                        <TextBlock FontSize="15" Text="{Binding OwnerNickname}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <ScrollViewer x:Name="MyScrollViewer" Background="#FFF8F8F8" VerticalScrollBarVisibility="Hidden" Margin="10,5,10,5" Grid.Row="1">
                    <ItemsControl ItemsSource="{Binding ChatMessagesForUI}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <Border CornerRadius="15" Margin="5" Padding="10">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsFromCurrentUser}" Value="True">
                                                        <Setter Property="Background" Value="#FADADD"/>
                                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsFromCurrentUser}" Value="False">
                                                        <Setter Property="Background" Value="White"/>
                                                        <Setter Property="HorizontalAlignment" Value="Left"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock TextWrapping="Wrap" Text="{Binding Content}" MaxWidth="300" FontSize="14"/>
                                            <TextBlock Text="{Binding SentAt}" FontSize="10" Foreground="Gray" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <DockPanel Grid.Row="2" Background="White" Margin="10,0,10,10">
                    <TextBox Text="{Binding MessageContent, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Height="30" Margin="0,0,5,0" KeyUp="TextBox_KeyUp" Width="338"/>
                    <Button Style="{StaticResource RegButtons}" Background="Transparent" BorderThickness="0" Width="40" Height="30" Click="Button_Click_1">
                        <Image Source="/Images/sendmes.png" Height="20" Width="20"/>
                    </Button>
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
