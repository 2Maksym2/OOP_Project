<UserControl x:Class="DealHubWPF.View.AdminPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DealHubWPF.View"
             mc:Ignorable="d" 
Height="600" Width="800">
    <Grid Background="#F5F5F5">
        <StackPanel Margin="20">

            <!-- Логотип + Іконки профілю -->
            <Border CornerRadius="15" BorderThickness="1" Background="#EAC4C4" Height="47"  Margin="0,0,0,10">
                <DockPanel Background="Transparent">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="/Images/updatelogo.png" Width="69" Height="53" Margin="0,0,5,0" />
                        <TextBlock Text=" DealHub" FontSize="18" FontWeight="Bold" DockPanel.Dock="Left" Margin="5,10,0,0" Width="103"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="Quit" Style ="{StaticResource CategoryButtons}" BorderThickness="0" Content="Вихід" Width="84" Margin="0,0,10,0" Click="Quit_Click"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- Списки -->
            <Border Margin="0,20,0,0" Background="#F2C1C1" CornerRadius="15" Height="54">
                <DockPanel Background="Transparent">
                    <TextBlock Text="Управління користувачами" VerticalAlignment="Center" FontSize="20" FontWeight="Bold" Margin="10,0,0,0"/>
                    <Button Command="{Binding AdminUsersListCommand}" Style="{StaticResource CategoryButtons}" HorizontalAlignment="Right" Content="Перейти" Margin="0,10,10,10" Width="108" />
                </DockPanel>
            </Border>
            <Border Margin="0,20,0,0" Background="#F2C1C1" CornerRadius="15" Height="54">
                <DockPanel Background="Transparent">
                    <TextBlock Text="Управління оголошеннями" VerticalAlignment="Center" FontSize="20" FontWeight="Bold" Margin="10,0,0,0"/>
                    <Button Command="{Binding AdminAdsListCommand}" Style="{StaticResource CategoryButtons}" HorizontalAlignment="Right" Content="Перейти" Margin="0,10,10,10" Width="108" />
                </DockPanel>
            </Border>
            <Border Margin="0,20,0,0" Background="#F2C1C1" CornerRadius="15" Height="54">
                <DockPanel Background="Transparent">
                    <TextBlock Text="Список скарг" VerticalAlignment="Center" FontSize="20" FontWeight="Bold" Margin="10,0,0,0"/>
                    <Button Command="{Binding AdminComplaintsCommand}" Style="{StaticResource CategoryButtons}" HorizontalAlignment="Right" Content="Перейти" Margin="0,10,10,10" Width="108" />
                </DockPanel>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>
