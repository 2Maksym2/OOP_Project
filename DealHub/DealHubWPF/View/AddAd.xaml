<UserControl x:Class="DealHubWPF.View.AddAd"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DealHubWPF.View"
             mc:Ignorable="d" d:DesignWidth="622"
  Background="White" Height="562">

    <Grid>
        <StackPanel Margin="20,20,20,10">

            <!-- Логотип + Іконки профілю -->
            <Border CornerRadius="15" BorderThickness="1" Background="#EAC4C4" Height="47"  Margin="0,0,0,10">
                <DockPanel Background="Transparent">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Command="{Binding RegUserCommand}" Style="{StaticResource RegButtons}" Background="Transparent" BorderThickness="0" Cursor="Hand">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Image Source="/Images/updatelogo.png" Width="69" Height="53" Margin="0,0,5,0" />
                                <TextBlock Text="DealHub" 
                                     FontSize="18" 
                                     FontWeight="Bold" 
                                     Margin="10,10,0,0" Width="103"
                                     Foreground="Black"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Command="{Binding ProfileCommand}" Style ="{StaticResource LogButtons}" BorderThickness="0" Background="Transparent">
                            <TextBlock Foreground="#65558F" FontFamily="Segoe MDL2 Assets" Text="&#xE77B;" FontSize="24" Margin="10"/>
                        </Button>
                        <Button Command="{Binding ChatsCommand}" Style ="{StaticResource LogButtons}" BorderThickness="0" Background="Transparent">
                            <TextBlock Foreground="#65558F"  FontFamily="Segoe MDL2 Assets" Text="&#xE8BD;" FontSize="24" Margin="10"/>
                        </Button>
                    </StackPanel>
                </DockPanel>
            </Border>


            <!-- Панель профілю -->
            <Border CornerRadius="15" BorderThickness="1" Background="#F0F0F0" Margin="10,5,10,0" Height="85" Width="713">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0" Height="100">
                    <Image Source="/Images/profileico.png" Width="106" Height="78" Margin="10"/>
                    <TextBlock Text="{Binding UserName}" VerticalAlignment="Center" FontSize="20" Margin="20,0,0,0"/>
                </StackPanel>
            </Border>

            <!-- Форма додавання товару -->
            <Border Margin="0,10,0,0" Background="#F5C8D0" CornerRadius="15" Padding="20" Height="358">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Фото -->
                    <Button Style="{StaticResource RegButtons}" Background="Transparent">

                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <Image Source="{Binding Image}" Width="155" Height="166" Margin="0,0,0,10"/>
                            <Button Style="{StaticResource CategoryButtons}"  Content="Вибрати фото" Click="ChooseImage_Click" Width="110" Margin="0,10,0,0" Height="24"/>
                        </StackPanel>
                    </Button>

                    <!-- Інформація про товар -->
                    <StackPanel Grid.Column="2" VerticalAlignment="Top">
                        <Label Content="Вкажіть назву продукту" />
                        <TextBox Background="Transparent" HorizontalAlignment="Left"  Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" FontWeight="Bold" Margin="0,0,0,5" Width="293"/>
                        <Label Content="Вкажіть ціну для продажу" />

                        <TextBox Background="Transparent" Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}" FontSize="13" Margin="0,0,0,3"/>
                        <Label Content="Оберіть категорію продукту" />
                        <!-- Випадаючий список -->
                        <ComboBox  ItemsSource="{Binding Categories}"   SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"  HorizontalAlignment="Left" Width="207" Text="Категорія">
                        </ComboBox>
                        <Label Content="Додайте детальний опис продукту (не обов’язково)"/>
                        <TextBox Background="Transparent" TextWrapping="Wrap" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" FontSize="13" Margin="0,5" Height="24"/>
                        <!-- Кнопка додавання -->
                        <Button Style="{StaticResource CategoryButtons}" Content="Додати оголошення" Width="160" Height="28" Background="#9B59B6" Foreground="White" Margin="0,50,0,0" BorderThickness="0" HorizontalAlignment="Right" Click="Button_Click" />

                    </StackPanel>
                </Grid>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>
